<!--p>
    <a class="btn btn-default" href="/users/new" role="button">
        <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
        Create a new user
    </a>
</p-->
{{#if users}}
<table class="table usertable">
    <thead>
        <tr>
            <th>Username</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Email</th>
            <th>Role</th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {{#each users}}
            <tr>
                <td>{{this.username}}</td>
                <td>{{this.firstName}}</td>
                <td>{{this.lastName}}</td>
                <td>{{this.email}}</td>
                <td>{{this.role}}</td>
                <td><form method="post" action="/users/edit"><input type="hidden" name="id" value="{{this._id}}"><button class="btn btn-link btn-sm" type="submit">Edit</button></form></td>
                <td><form method="post" action="/users/delete"><input type="hidden" name="id" value="{{this._id}}"><button class="btn btn-link btn-sm" type="submit">Delete</button></form></td>
            </tr>
        {{/each}}
    </tbody>
</table>
{{else}}
<div class="alert alert-info">No users found</div>
{{/if}}